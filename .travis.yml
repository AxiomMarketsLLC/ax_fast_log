# .travis.yml
# sudo: false
language: cpp

notifications:
  email: false
  slack: axiommarkets:uQPCDh2hZ26jzOx3TyWEnpDe

env:
  global:
    TRAVIS_BUILD_DIR="/home/build/travis/AxiomMarketsLLC/ax_fast_log"

before_install:
 
- sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
- sudo apt-get update -qq

install:
- sudo apt-get install -qq g++-4.8
- sudo apt-get install -qq pwd
- export CXX="g++-4.8"

script: ./test_travis.sh

after_failure:

  
after_success:
  - coveralls --exclude boost --exclude tests --gcov-options 'src/*.cpp -o'
